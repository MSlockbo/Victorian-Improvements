

decrease_autonomy_protectorate = {
	requires_approval = yes
	should_notify_third_parties = yes
	can_use_obligations = yes

	potential = {
		scope:target_country = {
			is_direct_subject_of = root

			is_subject_type = subject_type_protectorate

			NOT = {
				is_country_type = colonial
			}
		}
	}

	possible = {

		is_revolutionary = no

		scope:target_country = {
			is_revolutionary = no
			NOT = { has_truce_with = root }
		}

		is_at_war = no
		scope:target_country = { is_at_war = no }
	}

	accept_effect = {
		create_truce = { country = scope:target_country months = 12 }

		hidden_effect = {
			remove_diplomatic_pact = {
				country = scope:target_country
				type = protectorate
			}
		}

		create_diplomatic_pact = {
			country = scope:target_country
			type = dominion
		}
	}
}

decrease_autonomy_tributary = {
	requires_approval = yes
	should_notify_third_parties = yes
	can_use_obligations = yes

	potential = {
		scope:target_country = {
			is_direct_subject_of = root

			is_subject_type = subject_type_tributary

			NOT = {
				is_country_type = colonial
			}
		}
	}

	possible = {

		is_revolutionary = no

		scope:target_country = {
			is_revolutionary = no
			NOT = { has_truce_with = root }
		}

		is_at_war = no
		scope:target_country = { is_at_war = no }
	}

	accept_effect = {
		create_truce = { country = scope:target_country months = 12 }

		hidden_effect = {
			remove_diplomatic_pact = {
				country = scope:target_country
				type = tributary
			}
		}

		create_diplomatic_pact = {
			country = scope:target_country
			type = vassal
		}
	}
}

decrease_autonomy_dominion = {
	requires_approval = yes
	should_notify_third_parties = yes
	can_use_obligations = yes

	potential = {
		scope:target_country = {
			is_direct_subject_of = root

			is_subject_type = subject_type_dominion

			NOT = {
				is_country_type = colonial
			}
		}
	}

	possible = {

		is_revolutionary = no

		scope:target_country = {
			is_revolutionary = no
			NOT = { has_truce_with = root }
		}

		is_at_war = no
		scope:target_country = { is_at_war = no }
	}

	accept_effect = {
		create_truce = { country = scope:target_country months = 12 }

		hidden_effect = {
			remove_diplomatic_pact = {
				country = scope:target_country
				type = dominion
			}
		}

		create_diplomatic_pact = {
			country = scope:target_country
			type = puppet
		}
	}
}